# Get External IP / Internet Speed
alias myip="curl https://ipinfo.io/json" # or /ip for plain-text ip
alias speedtest="curl -s https://raw.githubusercontent.com/sivel/speedtest-cli/master/speedtest.py | python -"

# Quickly serve the current directory as HTTP
alias serve='ruby -run -e httpd . -p 8000' # Or python -m SimpleHTTPServer :)

# NOTE: On Q3 2021, Le Wagon decided to change the Web Dev curriculum default text editor
alias stt="echo 'Launching VS Code instead of Sublime Text... (cf ~/.aliases)' && code ."


# Get External IP / Internet Speed
alias myip="curl https://ipinfo.io/json" # or /ip for plain-text ip
alias speedtest="curl -s https://raw.githubusercontent.com/sivel/speedtest-cli/master/speedtest.py | python -"

# Quickly serve the current directory as HTTP
alias serve='ruby -run -e httpd . -p 8000' # Or python -m SimpleHTTPServer :)

# NOTE: On Q3 2021, Le Wagon decided to change the Web Dev curriculum default text editor
alias stt="echo 'Launching VS Code instead of Sublime Text... (cf ~/.aliases)' && code ."

alias create_project="""mkdir -p data;
mkdir -p notebooks;
mkdir -p project ;
mkdir -p tests;
mkdir -p project/app;
mkdir -p project/interface;
mkdir -p project/ml_logic;
touch requirements.txt;
touch .env;
touch .envrc;
touch .gitignore;
touch Makefile;
touch Readme.md;
touch setup.py;
touch  data/.keep;
touch  notebooks/.keep;
touch  project/.keep ;
touch  tests/.keep;
touch  project/app/.keep;
touch  project/interface/.keep;
touch  project/ml_logic/.keep;

echo "# Project Title"
>> README.md; echo "Add your project description here." >> README.md

"""


alias gpush='myls'

alias ozs='code ~/.zshrc'

alias sizeme="du -sh .* * | sort -rh"

alias lwenv="source ~/lewagonenv/.lewagonenv/bin/activate"

alias sda_report='firefox --new-tab "https://docs.google.com/spreadsheets/d/1VmgZw8V0KP9hOjaYxgvkFSbiRmvxBqv5cZR1WtPpkPU/edit?gid=2084985854#gid=2084985854" --new-tab "https://docs.google.com/spreadsheets/d/1JrIWgN9O1QiUufcyD1kPxkFA1KSuSKPtK2KwiboaLgA/edit?gid=0#gid=0" --new-tab "https://docs.google.com/spreadsheets/d/1dGF5PdrAVAXH20m86CAI-iGD0I-TP2jWF2gmy3YNv7w/edit?gid=1835126524#gid=1835126524"'


alias activate_local="source .venv/bin/activate"


alias update-dotfiles='zsh ~/code/Abdl242/dotfiles/update_repo_from_configs.sh'
